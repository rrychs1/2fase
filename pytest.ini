[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
timeout = 180
markers =
    anyio: marks tests as anyio-based async tests
    slow: marks slow tests (use -m 'not slow' to skip)
