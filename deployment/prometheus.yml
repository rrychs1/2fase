global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'algobot_core'
    static_configs:
      - targets: ['bot:8000']
        labels:
          engine: 'trading_v3'
